//! FreeCell Round-3 Investigation B (breadth) — needed-API audit. Scaffolding stub only.
//!
//! Phase B implements (see specs/projects/freecell-phase-3 architecture §5, functional
//! spec B): a checklist walk over IronCalc 0.7.1's public API, marking each entry
//! present / absent / workaround with a runtime probe or a source citation
//! (~/.cargo/registry/.../ironcalc*-0.7.1/), and a plan per gap. Headline: does IronCalc
//! produce a cell's DISPLAY string under a number format, or must FreeCell own
//! number-format rendering? Plus diff-list shape, sheet ops, defined names, view state,
//! cell extras, tokenizer; re-confirm merges / conditional formatting / dynamic arrays
//! as known-OPEN.

fn main() {
    println!("TODO(Phase B): needed-API audit vs IronCalc 0.7.1. Scaffolding stub.");
}
