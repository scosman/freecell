[package]
name = "bench_util"
version = "0.1.0"
edition = "2024"
description = "Timing, percentile, pass/fail-gating, and results-recording helpers for FreeCell Phase 1 benchmarks. Consumed read-only by later sub-projects."
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
